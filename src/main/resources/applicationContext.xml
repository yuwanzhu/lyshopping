<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:aop="http://www.springframework.org/schema/aop"
       xmlns:tx="http://www.springframework.org/schema/tx" xmlns:jdbc="http://www.springframework.org/schema/jdbc"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:task="http://www.springframework.org/schema/task"
       xsi:schemaLocation="
     http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd
     http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
     http://www.springframework.org/schema/task http://www.springframework.org/schema/task/spring-task.xsd
     http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx.xsd
     http://www.springframework.org/schema/aop http://www.springframework.org/schema/aop/spring-aop.xsd">
    <!--spring配置-->
    <!--打开扫描,且把控制层的扫描交给springmvc处理（隔离掉controller）-->
    <!--意思就是在spring容器中扫描所有的bean但是呢排除@Controller注解，-->
    <context:component-scan base-package="com.lyshopping" annotation-config="true">
        <context:exclude-filter type="annotation" expression="org.springframework.stereotype.Controller"></context:exclude-filter>
    </context:component-scan>

    <!--<context:annotation-config/>作用对已经注册的bean进行操作的一个配置，意思就是使用这个配置来激活已经注册的bean-->
    <aop:aspectj-autoproxy/>

    <!--二期新增springSchedule配置-->
    <context:property-placeholder location="classpath:./dev/datasource.properties"/>
    <task:annotation-driven/>

    <!--分离配置文件-->
    <import resource="applicationContext-datasource.xml"/>
    <import resource="applicationContext-spring-session.xml"/>


</beans>